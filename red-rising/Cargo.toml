[package]
name = "red-rising"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.19.1", features = ["dynamic_linking"] }

[dev-dependencies]
rstest = "0.27.0"

[profile.dev]
opt-level = 1
rpath = true

[profile.dev.package."*"]
opt-level = 3

